
<{{ element | default('div') }} class="{{ component_id }} {{ modifier_string(styleModifier, component_id ) }}" {% if target %}ref="{{ target }}"{% endif %}>
    <a class="{{ component_id }}__link" href="{{ href | default('#') }}" >{{ label }}</a>
</{{ element | default('div') }}>

<button class="{{ component_id }}">Submit</button>